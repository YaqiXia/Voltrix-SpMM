import setuptools
from setuptools import setup, find_packages


if __name__ == "__main__":
    setuptools.setup(
        name="voltrix",
        version="1.0.0",
        packeges=find_packages(),
        cmdclass={},
    )
