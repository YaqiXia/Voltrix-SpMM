import setuptools
from setuptools import setup, find_packages


if __name__ == "__main__":
    setuptools.setup(
        name="voltrix",
        version="1.0.0",
        packages=find_packages(),
        cmdclass={},
    )
